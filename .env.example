### Database connection (PostgreSQL)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

### NextAuth configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-random-secret"

### JWT key for NextAuth credentials provider (optional)
JWT_SECRET="replace-with-jwt-secret"

### VAPID keys for push notifications (optional for PWA)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""

### Storage configuration (for S3-compatible storage)
STORAGE_ENDPOINT="https://example-r2.storage.com"
STORAGE_BUCKET="ykvm"
STORAGE_ACCESS_KEY=""
STORAGE_SECRET_KEY=""

### TA rate and geofence defaults
TA_RATE_PER_KM="3"
GEOFENCE_RADIUS_METERS="10"
PING_INTERVAL_MINUTES="5"